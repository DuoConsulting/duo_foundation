{#
/**
 *******************************************************************************
 * See paragraphs/paragraph--card.html.twig
 *
 */
#}
{{ attach_library('duo_foundation/block--cta') }}
{%
  set classes = attributes.toArray().class|join(' ')
%}
{%
  include '@duo_components/block--cta/block--cta.twig' with {
    alignment: content.field_cta_text_alignment.0['#markup'],
    title: label,
    body: content.body,
    links: content.field_cta_links,
    defaults: classes,
    title_prefix: title_prefix,
    title_suffix: title_suffix,
  } only
%}

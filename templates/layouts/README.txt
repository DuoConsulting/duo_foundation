The templates provided here are intended to override the default templates for
layouts originally provided by the duo_layouts module. We do not define our
layouts in the theme, but instead rely on that module to define the layouts and
then we just override the markup here.